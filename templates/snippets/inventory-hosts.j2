
{% for node in m.cluster.nodes -%}
{{ node.name }} ansible_ssh_host={{ node.ip }}  ansible_ssh_user=root ansible_ssh_private_key_file={{m.env.keys_location}}/build_key		#  {{ node['hostname'] + "." + m.cluster.domain}}
{% endfor %}
